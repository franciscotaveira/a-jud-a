{
  "name": "public-intelligence-graph-brasil",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma de inteligência documental baseada em dados públicos verificáveis",
  "workspaces": [
    "packages/*",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "test": "npx -y tsx --test tests/m0_verification.test.ts",
    "typecheck": "npm run --workspaces --if-present typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
